<UserControl
    x:Class="LimpStats.Client.CustomControls.StudentGroupPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LimpStats.Client.CustomControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="200"
    d:DesignHeight="250"
    mc:Ignorable="d">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ListBox.Static.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Static.Border" Color="#FFABADB3"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Border" Color="#FFD9D9D9"/>
        <Style x:Key="ListBoxStyle2" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="{StaticResource ListBox.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ListBox.Static.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="1" SnapsToDevicePixels="true">
                            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Border}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsGrouping" Value="true"/>
                                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ListBoxItemStyle2" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Grid Margin="2" Height="23.5" Width="123" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <Rectangle Grid.ColumnSpan="2" Fill="#FF993333" HorizontalAlignment="Left" Height="20" Margin="2,0,-78.5,0" Stroke="{x:Null}" VerticalAlignment="Top" Width="190" RadiusX="2" RadiusY="4"/>
                                <ContentPresenter
                               
                                    Grid.Column="1"
                                    Margin="10.5,2.286,-6.5,8.5" HorizontalAlignment="Center" Width="119" VerticalAlignment="Center" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="TextElement.Foreground" Value="White"></Setter>
                <Setter Property="TextElement.FontFamily" Value="Century Gothic"></Setter>
            </Style>
    </UserControl.Resources>
    <Canvas>
        <Button
            Name="UpdateButton"
            Canvas.Right="0"
            Width="25"
            Height="25"
            HorizontalAlignment="Left"
            Click="ButtonClick_Update"
            Content="🔄" FontFamily="Century Gothic" Background="#FF993333" Foreground="#FFE8E8E8" />
        <Button
            Name="CardTitle"
            Width="159"
            Height="25"
            Background="#FF993333"
            Click="CardTitle_OnClick"
            DataContext="GrupTitle" FontFamily="Century Gothic" Foreground="#FFE8E8E8" Style="{DynamicResource ButtonStyle1}" />
        <Button
            Canvas.Top="25"
            Width="200"
            Height="19"
            Click="AddUserButton_OnClick"
            Content="AddUser" FontFamily="Century Gothic" Background="#FF993333" Foreground="#FFE8E8E8" />
        <ListBox
            Name="StudentList"
            Canvas.Top="44"
            Width="200"
            Height="206"
            MinHeight="50" Background="#FF4F7BBA" ItemContainerStyle="{DynamicResource ListBoxItemStyle2}" Style="{DynamicResource ListBoxStyle2}" />
        <Menu
            Canvas.Right="25"
            Width="15"
            Height="25"
            VerticalAlignment="Top">
            <MenuItem
                Width="15"
                Height="25"
                Header="⋮"
                RenderTransformOrigin="0.05,0.56" Background="#FF993333" Foreground="#FF993333">
                <MenuItem Header="Rename" />
                <MenuItem Header="Sort by">
                    <MenuItem Header="Nickname" />
                    <MenuItem Header="Name" />
                    <MenuItem Header="Points" />
                </MenuItem>
                <Separator />
                <MenuItem Click="ButtonDeleteCard" Header="Delete card" />
            </MenuItem>
        </Menu>
    </Canvas>
</UserControl>
